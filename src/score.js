export default class Score
{
    constructor(xPos,yPos, name, score)
    {
        this.xPos        = xPos;      //Posicion inicial en X
        this.yPos        = yPos;      // Posicion inicial en Y
        this.name        = name;      
        this.score       = score;  
    }
}